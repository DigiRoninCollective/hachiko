# Database (Required)
DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..."

# Supabase (Required)
NEXT_PUBLIC_SUPABASE_URL="https://..."
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY="..."

# Rate Limiting - Redis (Optional but recommended for production)
# Choose ONE of the following options:

# Option 1: Upstash Redis (REST API) - Works with Vercel, Cloudflare, Railway
# Get from: https://console.upstash.com or Vercel Dashboard → Storage → KV
# KV_REST_API_URL="https://your-redis.upstash.io"
# KV_REST_API_TOKEN="your-token"

# Option 2: Standard Redis (TCP) - Works with any Redis instance
# Self-hosted, Railway, Render, DigitalOcean, AWS ElastiCache, etc.
# REDIS_URL="redis://username:password@host:6379"
# Examples:
# - Local: redis://localhost:6379
# - Railway: redis://default:password@containers-us-west-xx.railway.app:6379
# - Render: redis://red-xxxxx:6379
# - AWS: redis://master.xxxxx.cache.amazonaws.com:6379

# If neither is set, falls back to in-memory (development only)

# Render Production Environment Variables
# =====================================
# These are automatically set by Render, but you can override if needed:
NODE_ENV=production
PORT=10000

# Optional Production Enhancements:
# SOLANA_RPC_URL="https://api.mainnet-beta.solana.com"
# JWT_SECRET="your-jwt-secret-here"
# WEBHOOK_SECRET="your-webhook-secret-here"

# External Service Keys (Optional):
# COINGECKO_API_KEY="your-coingecko-api-key"
# TELEGRAM_BOT_TOKEN="your-telegram-bot-token"
